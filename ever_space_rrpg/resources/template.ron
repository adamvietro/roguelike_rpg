Templates(
    entities : [
        Template(
            entity_type: Item,
            name : "Healing Potion", glyph : '!', levels : [ 0, 1, 2 ],
            effect: Some(Healing(6)),
            frequency: 2,
            description: Some("Restores 6 HP when used.")
        ),
        Template(
            entity_type: Item,
            name : "Dungeon Map", glyph : '{', levels : [ 0, 1, 2 ],
            effect: Some(MagicMap),
            frequency: 1,
            description: Some("Reveals the full layout of the current dungeon level.")
        ),
        Template(
            // prefab treasure only - prefab_only:true means this never
            // appears in the general ambient spawn pool, only via a
            // prefab's guaranteed 'S' marker (see spawn_prefab_weapon).
            // class: Barbarian - a Mage will never find one of these here;
            // it shares its guaranteed slot with the 3 Staff tiers below
            // instead. levels still gates which dungeon levels this tier
            // is eligible to be picked on.
            entity_type: Item,
            name : "Rusty Sword", glyph: 's', levels: [ 0],
            frequency: 1,
            base_damage: Some(1),
            class: Some("Barbarian"),
            prefab_only: true,
            description: Some("A dull, rusted blade - better than fists, barely.")
        ),
        Template(
            entity_type: Item,
            name : "Shiny Sword", glyph: 'S', levels: [ 1 ],
            frequency: 1,
            base_damage: Some(2),
            class: Some("Barbarian"),
            prefab_only: true,
            description: Some("A well-kept blade with a decent edge.")
        ),
        Template(
            entity_type: Item,
            name : "Huge Sword", glyph: '/', levels: [ 1, 2 ],
            frequency: 1,
            base_damage: Some(3),
            class: Some("Barbarian"),
            prefab_only: true,
            description: Some("A massive blade that hits like a truck.")
        ),
        Template(
            // Mage's weapon tier, parallel to the 3 Swords above - same
            // guaranteed prefab slot, same level gating, but class: Mage
            // so a Barbarian will never find one of these instead. Glyphs
            // are 0/1/2 placeholders (same idea as the class-select letter
            // icons) until real sprite art exists.
            entity_type: Item,
            name : "Wooden Staff", glyph: '0', levels: [ 0 ],
            frequency: 1,
            base_damage: Some(1),
            class: Some("Mage"),
            prefab_only: true,
            description: Some("A simple wooden staff, worn smooth from casting.")
        ),
        Template(
            entity_type: Item,
            name : "Silver Staff", glyph: '1', levels: [ 1 ],
            frequency: 1,
            base_damage: Some(2),
            class: Some("Mage"),
            prefab_only: true,
            description: Some("A silver-capped staff that channels magic more cleanly.")
        ),
        Template(
            entity_type: Item,
            name : "Arcane Staff", glyph: '2', levels: [ 1, 2 ],
            frequency: 1,
            base_damage: Some(3),
            class: Some("Mage"),
            prefab_only: true,
            description: Some("A staff humming with raw arcane power.")
        ),
        Template(
            // Rogue's weapon tier, parallel to the Swords/Staffs above -
            // same guaranteed prefab slot, same level gating, but class:
            // Rogue. Glyphs 4/5/6 continue the placeholder numbering the
            // Staffs started (0/1/2).
            entity_type: Item,
            name : "Rusty Dagger", glyph: '3', levels: [ 0 ],
            frequency: 1,
            base_damage: Some(1),
            class: Some("Rogue"),
            prefab_only: true,
            description: Some("A pitted little blade, more improvised than forged.")
        ),
        Template(
            entity_type: Item,
            name : "Silver Dagger", glyph: '4', levels: [ 1 ],
            frequency: 1,
            base_damage: Some(2),
            class: Some("Rogue"),
            prefab_only: true,
            description: Some("A balanced blade built for quick, precise strikes.")
        ),
        Template(
            entity_type: Item,
            name : "Venomous Dagger", glyph: '5', levels: [ 1, 2 ],
            frequency: 1,
            base_damage: Some(3),
            class: Some("Rogue"),
            prefab_only: true,
            description: Some("Its edge is faintly discolored - best not to lick it.")
        ),
        Template(
            // Battle loot only - levels:[] means this never appears as a
            // normal floor spawn, only via grant_random_battle_loot.
            entity_type: Item,
            name : "Deathblow", glyph: 'D', levels: [ ],
            technique: Some(DamageMultiplier(2)),
            frequency: 0,
            class: Some("Barbarian"),
            description: Some("A single devastating strike dealing double your normal attack damage.")
        ),
        Template(
            entity_type: Item,
            name : "Quick Attack", glyph: 'Q', levels: [ ],
            technique: Some(MultiHit(2)),
            frequency: 0,
            class: Some("Barbarian"),
            description: Some("Strike twice in the same turn before the enemy can react.")
        ),
        Template(
            entity_type: Item,
            name : "Counter Attack", glyph: 'C', levels: [ ],
            technique: Some(Counter(chance_percent: 65, multiplier: 3)),
            frequency: 0,
            class: Some("Barbarian"),
            description: Some("Skip your attack for a 65% chance to deal 3x damage back when the enemy strikes you.")
        ),
        Template(
            // Replaces the old "Garrote" technique - same DamageOverTime
            // effect, just a new name.
            entity_type: Item,
            name : "Rend", glyph: 'R', levels: [ ],
            technique: Some(DamageOverTime(damage: 2, turns: 3)),
            frequency: 0,
            class: Some("Barbarian"),
            description: Some("Wounds the enemy for 2 damage per turn over the next 3 turns.")
        ),
        Template(
            // Barbarian's AOE - see components::TechniqueEffect::AoeMultiHit.
            // Codepoint reservation only for now (glyph '≤', code 243 -
            // confirmed unused in the sprite map) - no art drawn yet, same
            // "reserve now, art later" pattern the Battle Arena session's
            // ability icons went through.
            entity_type: Item,
            name : "Whirlwind", glyph: '≤', levels: [ ],
            technique: Some(AoeMultiHit(hits: 3, bonus_damage: 2)),
            frequency: 0,
            class: Some("Barbarian"),
            description: Some("Spin and strike every enemy 3 times, each hit dealing your normal attack damage plus 2.")
        ),
        Template(
            // Mage battle loot - same levels:[]/frequency:0 pattern as the
            // Barbarian techniques above (battle-loot only).
            entity_type: Item,
            name : "Fireball", glyph: 'F', levels: [ ],
            technique: Some(FlatDamage(2)),
            frequency: 0,
            class: Some("Mage"),
            description: Some("Hurls a bolt of fire for 2 damage, plus your staff's damage.")
        ),
        Template(
            entity_type: Item,
            name : "Burn", glyph: 'b', levels: [ ],
            technique: Some(DamageOverTime(damage: 1, turns: 3)),
            frequency: 0,
            class: Some("Mage"),
            description: Some("Sets the enemy alight for 1 damage per turn over the next 3 turns.")
        ),
        Template(
            // Ice Armor is used out of combat, like Invisible Cloak -
            // "Mages buff before battle." +2 Defense for the next 10
            // enemy hits actually landed (counts down on hits absorbed,
            // not turns) - see components::IceArmored /
            // battle::resolve_enemy_attack. Numbers are a first guess,
            // easy to retune here with no code change.
            entity_type: Item,
            name : "Ice Armor", glyph: 'A', levels: [ ],
            effect: Some(IceArmor(defense_bonus: 2, attacks: 10)),
            frequency: 0,
            class: Some("Mage"),
            description: Some("Encases you in ice, adding 2 Defense for your next 10 hits taken. Use it before you walk into a fight.")
        ),
        Template(
            // Battle loot only (levels:[]/frequency:0) - NOT an ambient
            // floor spawn. grant_random_battle_loot filters its candidate
            // pool by class alone (not by BattleItem/technique/effect), so
            // this needed no code changes to become loot - only data.
            // Not a BattleItem/Technique - it stays off the battle menu
            // and shows up in the ordinary dungeon-view item list once
            // won, since it's explicitly an out-of-combat ability (see
            // components::Invisible / systems/player_input.rs).
            entity_type: Item,
            name : "Invisible Cloak", glyph: 'I', levels: [ ],
            effect: Some(Invisibility(20)),
            frequency: 0,
            class: Some("Mage"),
            description: Some("Wear it to become unseen for 20 moves - enemies can't touch you, but you can't push past them either.")
        ),
        Template(
            // Mage's AOE - see components::TechniqueEffect::AoeMultiHit.
            // "Basic damage" (no bonus_damage, unlike Whirlwind) but more
            // hits than any other class's AOE - a Mage's own attack
            // damage is typically low (staff-scaled, Defense -1), so the
            // hit count carries this one rather than a per-hit bonus.
            // Codepoint reservation only for now (glyph '÷', code 246 -
            // confirmed unused in the sprite map) - no art drawn yet.
            entity_type: Item,
            name : "Blizzard", glyph: '÷', levels: [ ],
            technique: Some(AoeMultiHit(hits: 6, bonus_damage: 0)),
            frequency: 0,
            class: Some("Mage"),
            description: Some("Summon a storm of ice, striking every enemy 6 times for your normal attack damage.")
        ),
        Template(
            // Rogue battle loot - same levels:[]/frequency:0 pattern as
            // the Barbarian/Mage techniques above (battle-loot only).
            // Same DamageOverTime shape as Rend/Burn, just Rogue's own
            // numbers/name.
            entity_type: Item,
            name : "Garrote", glyph: '≡', levels: [ ],
            technique: Some(DamageOverTime(damage: 2, turns: 3)),
            frequency: 0,
            class: Some("Rogue"),
            description: Some("Wounds the enemy for 2 damage per turn over the next 3 turns.")
        ),
        Template(
            // +70% evasion for the next 3 enemy attacks faced, stacking
            // additively with Rogue's base Evasion (10%, see
            // spawner::class_base_stats) for 80% total while active - see
            // components::Evasion / battle::resolve_enemy_attack.
            entity_type: Item,
            name : "Dodge", glyph: 'd', levels: [ ],
            technique: Some(Evade(chance_percent: 70, turns: 3)),
            frequency: 0,
            class: Some("Rogue"),
            description: Some("Boosts your evasion by 70% for the next 3 enemy attacks.")
        ),
        Template(
            // Stealth is used out of combat, like Invisible Cloak, but
            // does NOT block a bump-into-enemy from starting a battle -
            // it starts the battle as an ambush instead (forced first
            // turn + 3x damage on the opening action). See
            // components::Stealthed / systems/player_input.rs /
            // battle::Battle::sneak_attack.
            entity_type: Item,
            name : "Stealth", glyph: '·', levels: [ ],
            effect: Some(Stealth(20)),
            frequency: 0,
            class: Some("Rogue"),
            description: Some("Move unseen for 20 moves. Walk into an enemy while active and you'll strike first for 3x damage.")
        ),
        Template(
            // Rogue's AOE - see components::TechniqueEffect::AoeMultiHit.
            // Same shape as a single-target MultiHit, just applied to
            // every enemy in the fight - "Multi-hit attack" as a group
            // effect. Codepoint reservation only for now (glyph '≥',
            // code 242 - confirmed unused in the sprite map) - no art
            // drawn yet.
            entity_type: Item,
            name : "Flurry", glyph: '≥', levels: [ ],
            technique: Some(AoeMultiHit(hits: 3, bonus_damage: 0)),
            frequency: 0,
            class: Some("Rogue"),
            description: Some("A blur of strikes - hit every enemy 3 times for your normal attack damage.")
        ),
        Template(
            // Debug class only - never a floor spawn (levels:[]) or
            // random loot (frequency:0), only ever granted directly via
            // resources/starting_kits.ron. Sets TurnState::Victory the
            // instant it's used - see components::ProvidesEffect::DebugWin
            // / systems/use_items.rs.
            entity_type: Item,
            name : "Victory", glyph: '?', levels: [ ],
            effect: Some(DebugWin),
            frequency: 0,
            class: Some("Debug"),
            description: Some("Debug: instantly wins the run, as if the Amulet of Yala had just been claimed.")
        ),
        Template(
            // Sets TurnState::GameOver - see
            // components::ProvidesEffect::DebugLose.
            entity_type: Item,
            name : "Defeat", glyph: '?', levels: [ ],
            effect: Some(DebugLose),
            frequency: 0,
            class: Some("Debug"),
            description: Some("Debug: instantly ends the run as a loss, as if Health had just hit 0.")
        ),
        Template(
            // Sets TurnState::NextLevel - see
            // components::ProvidesEffect::DebugNextLevel. Granted x2 in
            // the starting kit (resources/starting_kits.ron), so the
            // default kit alone can reach the deepest normal dungeon
            // level (0 -> 1 -> 2) without any real exploration.
            entity_type: Item,
            name : "Next Level", glyph: '?', levels: [ ],
            effect: Some(DebugNextLevel),
            frequency: 0,
            class: Some("Debug"),
            description: Some("Debug: instantly advances to the next dungeon level, as if the Exit tile had just been reached.")
        ),
        Template(
            // Amazon's weapon tier, parallel to the Swords/Staffs/Daggers
            // above - same guaranteed prefab slot, same level gating, but
            // class: Amazon. Glyphs w/x/y continue the placeholder
            // numbering/lettering the other tiers started.
            entity_type: Item,
            name : "Wooden Spear", glyph: 'X', levels: [ 0 ],
            frequency: 1,
            base_damage: Some(1),
            class: Some("Amazon"),
            prefab_only: true,
            description: Some("A crude wooden spear, sharpened by hand.")
        ),
        Template(
            entity_type: Item,
            name : "Bronze Spear", glyph: 'Y', levels: [ 1 ],
            frequency: 1,
            base_damage: Some(2),
            class: Some("Amazon"),
            prefab_only: true,
            description: Some("A well-balanced bronze-tipped spear.")
        ),
        Template(
            entity_type: Item,
            name : "Iron Spear", glyph: 'Z', levels: [ 1, 2 ],
            frequency: 1,
            base_damage: Some(3),
            class: Some("Amazon"),
            prefab_only: true,
            description: Some("A heavy iron spear that punches clean through armor.")
        ),
        Template(
            // Amazon out-of-combat item - see
            // components::ProvidesEffect::RangedStrike /
            // systems/use_items.rs. Damages the nearest currently-visible
            // enemy for player attack + weapon damage + 2, with no battle
            // starting at all. Battle-loot only (levels:[]/frequency:0),
            // same pattern as every other class-specific special item -
            // also granted x1 in the Amazon starting kit (see
            // resources/starting_kits.ron), which bypasses these filters.
            entity_type: Item,
            name : "Throw Spear", glyph: 'J', levels: [ ],
            effect: Some(RangedStrike(2)),
            frequency: 0,
            class: Some("Amazon"),
            description: Some("Hurls a spear at the nearest enemy you can see for your attack + weapon damage + 2 - no battle starts.")
        ),
        Template(
            // In-battle technique - see
            // components::TechniqueEffect::WarCry / battle::resolve_enemy_attack.
            // Deliberately a random 1-2 reduction per hit, per design -
            // the one effect in this project that isn't a flat number.
            entity_type: Item,
            name : "Battle Cry", glyph: 'U', levels: [ ],
            technique: Some(WarCry(min_reduction: 1, max_reduction: 2, attacks: 3)),
            frequency: 0,
            class: Some("Amazon"),
            description: Some("Rally yourself - the enemy deals 1-2 less damage per hit for your next 3 hits taken.")
        ),
        Template(
            // In-battle technique - see
            // components::TechniqueEffect::PoisonStrike. An immediate hit
            // plus a lingering wound, reusing the same DamageOverTime
            // machinery Rend/Burn/Garrote already use for the DOT half.
            entity_type: Item,
            name : "Poison Spear", glyph: 'P', levels: [ ],
            technique: Some(PoisonStrike(initial: 1, dot_damage: 2, dot_turns: 3)),
            frequency: 0,
            class: Some("Amazon"),
            description: Some("A venomous jab: 1 immediate damage, then 2 more damage per turn for the next 3 turns.")
        ),
        Template(
            // Amazon's AOE - see components::TechniqueEffect::AoeMultiHit.
            // "Current weapon damage" per hit is the same normal-attack-
            // damage formula every other class's AOE uses (base Damage +
            // carried weapon Damage) - the description just calls out
            // that a better spear tier scales this up, same as any other
            // Amazon attack. Codepoint reservation only for now (glyph
            // '√', code 251 - confirmed unused in the sprite map) - no
            // art drawn yet.
            entity_type: Item,
            name : "Javelin Volley", glyph: '√', levels: [ ],
            technique: Some(AoeMultiHit(hits: 9, bonus_damage: 0)),
            frequency: 0,
            class: Some("Amazon"),
            description: Some("Hurl a volley of javelins, striking every enemy 9 times for your current weapon damage.")
        ),
        Template(
            // Amazon out-of-combat item - see
            // components::ProvidesEffect::PlaceTrap / systems/traps.rs.
            // Places a hazard at your current tile; the first enemy to
            // step on it takes 5 damage, then it's consumed.
            entity_type: Item,
            name : "Trap", glyph: 'T', levels: [ ],
            effect: Some(PlaceTrap(5)),
            frequency: 0,
            class: Some("Amazon"),
            description: Some("Place a hidden trap on your tile. The first enemy that steps on it takes 5 damage.")
        ),
        Template(
            // Hunter's weapon tier, parallel to the Swords/Staffs/
            // Daggers/Spears above - same guaranteed prefab slot, same
            // level gating, but class: Hunter. Glyphs 6/7/8 are the
            // existing Bow-tier dungeon-font cells (see
            // Dungeon_Font_Glyph_to_Cell_Map.md) - already custom art,
            // just never wired up to an actual item until now.
            entity_type: Item,
            name : "Wooden Bow", glyph: '6', levels: [ 0 ],
            frequency: 1,
            base_damage: Some(1),
            class: Some("Hunter"),
            prefab_only: true,
            description: Some("A simple bow, carved from a single branch.")
        ),
        Template(
            entity_type: Item,
            name : "Hunting Bow", glyph: '7', levels: [ 1 ],
            frequency: 1,
            base_damage: Some(2),
            class: Some("Hunter"),
            prefab_only: true,
            description: Some("A sturdy bow built for real game, not target practice.")
        ),
        Template(
            entity_type: Item,
            name : "Great Bow", glyph: '8', levels: [ 1, 2 ],
            frequency: 1,
            base_damage: Some(3),
            class: Some("Hunter"),
            prefab_only: true,
            description: Some("A powerful longbow with serious range and punch.")
        ),
        Template(
            // Hunter out-of-combat item, same shape as Amazon's Throw
            // Spear above - see ProvidesEffect::RangedStrike /
            // systems/use_items.rs. Damages the nearest visible enemy
            // for weapon + base damage + 1, no fight starts.
            entity_type: Item,
            name : "Shoot", glyph: 'H', levels: [ ],
            effect: Some(RangedStrike(1)),
            frequency: 0,
            class: Some("Hunter"),
            description: Some("Fire at the nearest visible enemy for your attack + weapon damage + 1 - no battle starts.")
        ),
        Template(
            // Hunter out-of-combat item - see
            // components::ProvidesEffect::PlaceFreezeTrap /
            // components::FreezeTrap / systems/traps.rs. Same
            // placed-hazard shape as Amazon's Trap above, but freezes
            // instead of damaging - see components::Frozen /
            // systems/chasing.rs (a frozen enemy can't move or attack)
            // and systems/entity_render.rs (frozen enemies render blue).
            entity_type: Item,
            name : "Freeze Trap", glyph: 'T', levels: [ ],
            effect: Some(PlaceFreezeTrap(40)),
            frequency: 0,
            class: Some("Hunter"),
            description: Some("Place a hidden trap on your tile. The first enemy that steps on it is frozen solid for 40 turns.")
        ),
        Template(
            // In-battle technique - see
            // components::TechniqueEffect::DamageOverTime. Reuses the
            // exact same DOT machinery Rend/Burn/Garrote already use -
            // no code change needed, just this data entry.
            entity_type: Item,
            name : "Poison Shot", glyph: 'p', levels: [ ],
            technique: Some(DamageOverTime(damage: 2, turns: 3)),
            frequency: 0,
            class: Some("Hunter"),
            description: Some("A venomous arrow: 2 damage per turn for the next 3 turns.")
        ),
        Template(
            // In-battle technique - see
            // components::TechniqueEffect::Stun / battle::resolve_enemy_attack.
            // A percent-chance, multi-turn version of the same
            // "enemy skips its attack" mechanic Feint below uses
            // guaranteed-but-briefer.
            entity_type: Item,
            name : "Stun", glyph: '±', levels: [ ],
            technique: Some(Stun(chance_percent: 60, turns: 3)),
            frequency: 0,
            class: Some("Hunter"),
            description: Some("60% chance to stun the enemy, preventing it from attacking for the next 3 turns.")
        ),
        Template(
            // In-battle technique - see
            // components::TechniqueEffect::Feint. Always lands, unlike
            // Stun above, but only holds off the enemy's very next
            // single attack - played instead of attacking, to sit out
            // one hit for free rather than gambling on Defend.
            entity_type: Item,
            name : "Feint", glyph: 'f', levels: [ ],
            technique: Some(Feint),
            frequency: 0,
            class: Some("Hunter"),
            description: Some("Play dead to guarantee the enemy's next attack misses entirely.")
        ),
        Template(
            // Hunter's AOE - see components::TechniqueEffect::AoeMultiHit.
            // Same shape as Amazon's Javelin Volley, just Hunter's own
            // name/flavor (arrows instead of javelins) - "current weapon
            // damage" per hit. Codepoint reservation only for now (glyph
            // '■', code 254 - confirmed unused in the sprite map) - no
            // art drawn yet.
            entity_type: Item,
            name : "Arrow Volley", glyph: '■', levels: [ ],
            technique: Some(AoeMultiHit(hits: 9, bonus_damage: 0)),
            frequency: 0,
            class: Some("Hunter"),
            description: Some("Loose a volley of arrows, striking every enemy 9 times for your current weapon damage.")
        ),
        Template(
            entity_type: Enemy,
            name : "Goblin", glyph : 'g', levels : [ 0 ],
            hp : Some(1),
            frequency: 3,
            base_damage: Some(1),
            speed: Some(9)
        ),
        Template(
            entity_type: Enemy,
            name : "Orc", glyph : 'o', levels : [ 0, 1, 2 ],
            hp : Some(2),
            frequency: 2,
            base_damage: Some(1),
            speed: Some(7)
        ),
        Template(
            entity_type: Enemy,
            name : "Ogre", glyph : 'O', levels : [ 1, 2 ],
            hp : Some(5),
            frequency: 1,
            base_damage: Some(2),
            speed: Some(4)
        ),
        Template(
            entity_type: Enemy,
            name : "Ettin", glyph : 'E', levels : [ 2 ],
            hp : Some(10),
            frequency: 1,
            base_damage: Some(3),
            speed: Some(2)
        ),
        Template(
            // PLACEHOLDER STATS - guards the Exit tile on level 0 (see
            // spawner::spawn_boss / Templates::spawn_boss). Numbers here
            // are a rough first pass, deliberately just "somewhat
            // stronger than Goblin/Orc" so the placement mechanism is
            // testable now - real balancing is a separate pass.
            entity_type: Enemy,
            name : "Goblin Chieftain", glyph : 'G', levels : [ 0 ],
            hp : Some(6),
            frequency: 1,
            base_damage: Some(2),
            speed: Some(6),
            boss_only: true
        ),
        Template(
            // PLACEHOLDER STATS - guards the Exit tile on level 1.
            entity_type: Enemy,
            name : "Orc Warlord", glyph : 'K', levels : [ 1 ],
            hp : Some(10),
            frequency: 1,
            base_damage: Some(3),
            speed: Some(5),
            boss_only: true
        ),
        Template(
            // PLACEHOLDER STATS - guards the Amulet of Yala on level 2,
            // the final boss.
            entity_type: Enemy,
            name : "Ettin Overlord", glyph : 'V', levels : [ 2 ],
            hp : Some(16),
            frequency: 1,
            base_damage: Some(4),
            speed: Some(3),
            boss_only: true
        ),
    ],
)